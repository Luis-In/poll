import { makeStyles } from "@mui/styles"; 

export default makeStyles(() => ({
    root: {
        maxWidth: "100%",
        display: "flex",
        gap: "10%",
        flexDirection: "column",
        justifyContent: "start",
        alignItems: "center",
        height: "100vh"
    }
}))