import { makeStyles } from "@mui/styles"

export default makeStyles(() => ({
    root: {
        display: "flex",
        flexDirection: "column",
        gap: "1.5rem",
        maxWidth: "90%",
        margin: "auto",
        padding: "1.5rem",
    }
}))